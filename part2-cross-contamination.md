*Bitcoin ETFs Are Not Safu[^safu] — Part 2*

*~1,500 words / ~3 pages*

# Cross-Contamination: When Is Bitcoin Just Another Shitcoin?

Coinbase will tell you — in the same reassuring language every big custodian uses — that your Bitcoin sits behind "bank-grade," "institutional-grade," cryptographically bulletproof custody. Air-gapped HSMs. Geographically sharded keys. Threshold signatures audited by respected third parties. It's a good story, and it isn't a lie.

It's also not the whole story. Here's a sharper question. Is Coinbase a Bitcoin company that also lists a few vetted tokens — or a 500-asset trading shop that happens to custody a supermajority of America's ETF Bitcoin, the Bitcoin brand doing the reassuring while the engineering treats BTC as line item #1 of 500? You don't have to guess. The answer is in the custody code they published.[^1]

## What Coinbase actually runs

Coinbase's custody backbone is `cb-mpc` — a **multi-party computation** (MPC) cryptography library it open-sourced and had independently audited by Cure53.[^1][^2] And it's worth saying plainly, because everything after this is a criticism: the math is real, the audit happened. But whether the crypto is fixed or not is beside the point. Bitcoin never needed this machine in the first place.

Read the code, and notice what a signature actually is. When Coinbase says "MPC," the "multiple parties" are multiple *machines* — key-shards on servers, running a protocol too fast and too chatty for a human to sit inside. Producing one signature is a multi-round ceremony: the shard-holders commit to secret values, run an oblivious-transfer exchange to *multiply* their shares together, prove to each other they did it honestly, and only then assemble a signature — roughly eight machine-to-machine messages just to synthesize one signature.[^3] The math guarantees no single *machine* can move your Bitcoin. Whether a single *person* can is a different question — one answered by a policy layer they don't publish, and the subject of Part 3.

## The tool that's sitting right there

Bitcoin has verified threshold custody natively — on its own blockchain, in consensus, since 2012. It's called multisig. You lock coins to *k*-of-*n* public keys, and the network itself refuses to move them without *k* signatures. Each key signs **independently**; there is no ceremony, no oblivious transfer, no share-multiplication, no off-chain protocol to get right. The threshold lives in Bitcoin's own rules, not in a signature that has to be manufactured. This isn't exotic or new — it's how serious Bitcoin custody is actually done. Unchained and Onramp have run client-held on-chain multisig for years[^6] — and it isn't only Bitcoin-only shops: BitGo, a top-tier institutional custodian, still runs native on-chain multisig for Bitcoin, pitching the on-chain audit trail (provably, which keys signed) as its edge over MPC's off-chain opacity.[^bitgo]

And here is the whole argument in one move. Coinbase runs an elaborate off-chain machine to *manufacture* one threshold signature. Bitcoin will do the same job on-chain, for free, with keys that never talk to each other — Coinbase could hold all three keys of a 3-of-3 in its own vaults and get the identical "no single machine can move it" guarantee,[^redoctober] with a decade of battle-testing and none of the oblivious-transfer machinery. The safest computation is the one you never perform.

Be straight about the one real cost the other way. On-chain multisig isn't free: it *reveals* its own structure — the keys, the threshold — every time it spends, and costs a little more in fees. That on-chain visibility is exactly why a privacy-conscious custodian reaches for the single-signature look of MPC. But Bitcoin has an answer to even that: since Taproot, threshold schemes like **FROST** produce an ordinary-looking single Schnorr signature, hiding the structure while keeping the whole thing Bitcoin-native.[^5] Those are still maturing — reference code marked non-production, standardization a live draft — so a custodian switching *today* would trade a hardened pipeline for something newer. But notice what that *isn't*: it isn't Bitcoin lacking the tool. Plain multisig has shipped for over a decade. Nobody was forced into off-chain MPC by a missing feature; they were early, and nobody's since made switching a priority.

One more edge the other way, named so this doesn't read as a free lunch: MPC can *rotate* its shares — retire a departed employee's key, or refresh every share to defeat a slow, one-server-at-a-time breach — without a single on-chain transaction, where native multisig would have to move the coins to a fresh address to do the same. A genuine advantage. And a pointed one: that refresh is the very multiparty key-refresh where Cure53 found the library's lone *high-severity* hole.[^2] The one thing MPC does that multisig can't is also the part that broke.

## The generality is the contamination

Why the round-heavy off-chain machinery instead of Bitcoin's own on-chain multisig? The honest answer isn't incompetence. It's that Coinbase custodies *hundreds* of assets, and almost none of them have anything like Bitcoin's native multisig to fall back on. A single oblivious-transfer MPC protocol — the same generic key-splitting machinery, indifferent to which chain it happens to be signing for — signs for *every* one of them with one codebase. That is its selling point: one universal pipeline for the whole 500-asset book.

Sit with that, because it *is* the cross-contamination thesis stated in the protocol's own design goals. The tool Coinbase chose is optimized for being **asset-agnostic** — and Bitcoin, the one asset in the vault with its own better-fitting native custody, gets the generic one anyway. Its generality is a virtue for a multi-asset trading shop. It's the contamination for Bitcoin. And nobody in the custody chain — the ETF issuer, the bank administering its cash, the custodian holding the coins — has a Bitcoin-native instinct to override it, because not one of them is a Bitcoin-native company.

And it shows on-chain. Because the pipeline manufactures one ordinary signature per input, the coins move as plain *single-signature* spends — ECDSA on the secp256k1 curve, the very signing path cb-mpc also runs for Ethereum and its ERC-20 book — from `3…` and `bc1q` addresses that chain-analysts attribute to its custodian.[^4] Not Bitcoin's own Taproot/Schnorr, which the library *also* supports but which is Bitcoin-specific; not on-chain multisig, which the network could verify for itself. That is the contamination made visible: the one asset with its own native custody path, signed through the lowest-common-denominator machinery it shares with five hundred others — its Bitcoin-specific tooling left on the shelf, the whole multi-party apparatus hidden off-chain, for the largest pile of institutional Bitcoin on Earth.

There's a subtler tell, too. The ETF's Bitcoin reserve addresses aren't published for anyone to verify; the reason offered, via Bloomberg's Eric Balchunas, is "spam" — dust and junk coins sent to a known address.[^balchunas] But a dusted address stays perfectly auditable, so that only bites on its stronger reading: that stray UTXOs are a genuine burden to manage — which is true only when the Bitcoin lacks the coin-control a Bitcoin-native custodian has by default. Either "spam" is cover for opacity (Part 4's subject), or it's a quiet admission the tooling was never built around Bitcoin's UTXO model.

## The surface is real, and it's their own

You don't have to imagine the machinery failing. Coinbase paid Cure53 to audit their own library — and the auditors found a *high-severity* hole in the very routine that refreshes the keys guarding this Bitcoin: a small-subgroup attack on the multiparty key-refresh.[^2] They fixed it. But the lesson isn't that it's fixed; it's that even the builders, paying experts to break it, keep finding the seams. On-chain multisig has fewer seams because it has fewer parts — most of them Bitcoin's own, already battle-tested by the network itself.

This is the difference between marketing security and first-principles security, made concrete. Every extra message in that ceremony, every zero-knowledge proof, every dependency the oblivious-transfer layer drags in, is one more place a bug can hide — and a bug in the *shared* ECDSA machinery is a bug on the *Bitcoin* path, even though Bitcoin's own custody tools had nothing to do with it. A minimal on-chain-multisig Bitcoin stack simply would not contain the vulnerable part.

## It goes up a level, too

The instinct to run one universal pipeline doesn't stop at Coinbase — it's reinforced from above. BlackRock isn't a Bitcoin company; it's a generalist that manages trillions across every asset class, and crypto is one product line. Its Bitcoin and Ethereum ETFs — IBIT and ETHA — use the *identical* custody arrangement: Coinbase Custody holding the assets, BNY Mellon on cash administration, one vendor relationship, undifferentiated by asset.[^8] Bitcoin and Ethereum are not the same kind of thing — one is a simple bearer asset with a purpose-built, on-chain custody path sitting unused, the other a smart-contract environment with an entirely different risk profile. Treating them as interchangeable line items serviced by one custody relationship isn't a coincidence. It's what a generalist does with a product category it wasn't built around.

## What the universal pipeline drags in

Beyond the multi-round signing protocol itself, an oblivious-transfer MPC stack pulls in a real tree of dependencies — OT and OT-extension code, zero-knowledge-proof frameworks, multi-round networking.[^3] Every one is a place someone could quietly plant something, the pattern behind the 2023 Ledger Connect Kit compromise, where a single phished credential pushed malicious code through a trusted supply chain.[^7]  An on-chain multisig drags in almost none of it: the heavy lifting is done by Bitcoin's own consensus, code that tens of thousands of nodes already run and scrutinize. That's not a risk unique to Coinbase — but it's a real cost of running one sprawling, dependency-heavy pipeline built to serve five hundred assets at once, instead of leaning on the small, purpose-built machinery the one asset that needs the strongest guarantees already ships with.

(The *other* obvious question — not whether the machine is fit to purpose, but whether a single human hand could quietly direct it — is a different kind of problem, and the subject of Part 3.)

## The actual verdict

None of this means Coinbase's cryptography is snake oil, or its cryptographers fools. They chose a good, modern, audited protocol. Nobody's Bitcoin has evaporated because of a one-size-fits-all MPC bug. (Yet.)

But "defensible" and "the best available option for Bitcoin specifically" are different claims, and the industry has quietly let the first stand in for the second. If you're trusting a custodian with real money, the question to ask is direct: does my Bitcoin's threshold live on Bitcoin's own blockchain, or does it ride the same universal off-chain pipeline as your two-hundredth-ranked altcoin? For Coinbase today, the answer is the pipeline. Your Bitcoin's key security is cross-contaminated with the operational needs of assets that don't deserve to be anywhere near it — and if a hack ever traces to the machinery itself, rather than to the human hand directing it (that's Part 3), this is where it will start, not a broken proof.

And defensible-or-not is the shallow cut. The obvious objection — *every* custodian holds many assets, and can do it perfectly well — is right, and it isn't the point. The sin isn't that Bitcoin shares a vault with gold or a hundred tokens; a custodian can hold all of them and still handle each with its own best tooling. The sin is that Bitcoin — the one asset with purpose-built, transparent, decade-tested native custody — is run through the generic pipeline anyway, the machinery built for the four-hundred-odd assets that *have* no native alternative, because one pipeline for all five hundred is cheaper. Bitcoin isn't contaminated by its neighbors. It's contaminated by being treated as one of them.

So the fix has two halves, and neither is a cleverer machine. Sign the Bitcoin with Bitcoin's own tooling — the native multisig that has sat there, ignored, for a decade. And break the *pile*: into many capped, genuinely independent custody cells — separate keys, separate code, separate teams — so no single failure, a bug or a breach or a bankruptcy or a subpoena, can move more than its slice. Spread those cells across more than one institution and the corporate failures are bounded too. Keep the custodian's scale; kill the single target.




Bitcoin is not a shitcoin. It doesn't deserve to ride the same rails, through the same round-heavy machinery, as the other 499 assets sharing a vault with it. Institutions that demand segregated custody and independent audit trails for every other asset class they touch have been remarkably willing to accept "trust the brand" for the one asset where the stakes are highest. That's not diligence. It's habit. It's time to stop being polite about it — and to name the fix that reaches the root: not a cleverer machine, but custody no single company can lose. That's where this series is going. %% I think this is basically correct for the series but confusing where it is placed. %%


## Whose job is it to write it down?

Step back from the tribalism, because the underlying issue isn't tribal. It's an *unforced error.* The most protocol-heavy machinery Coinbase runs — the roughly eight-message, oblivious-transfer threshold ECDSA, with its whole dependency tree — is pointed at its single *largest* asset. By value, Bitcoin isn't line item #500 in that vault; it's the biggest thing in it, carrying the attack surface built for the long tail.[^aum] The bitcoin native alternative — multisig, no signing ceremony at all, a decade in production — would carry almost none of it. Cure53 already found a hole in the "crypto" shared machinery Bitcoin never needed. By any ordinary standard, that is a material risk.

And it is written down *nowhere.* Not in the prospectus, whose risk factors speak of custody only in the generic — "we rely on the Custodian; keys can be lost" — and never of this. Not by the custodian, for whom the opacity is the point. Not by NYDFS, whose examinations are sealed by statute. Not by the auditor, who certifies the controls run *as described*, never that the architecture is the right one. A material risk to the largest asset in a hundred-billion-dollar fund, and no one owns it. Whose job is it to write it down?

That's the question the rest of this series is for. And the answer isn't a decree, or a demand to re-engineer anything — it's the modest one: *say it out loud,* in the prospectus, where material risks are supposed to live, under the eye of the regulator that already asked these very issuers, in 2023, who even verifies the bitcoin exists.

---

[^1]: [coinbase/cb-mpc on GitHub](https://github.com/coinbase/cb-mpc); [Coinbase, "Introducing Coinbase's Open Source MPC Cryptography Library"](https://www.coinbase.com/blog/introducing-coinbases-open-source-mpc-cryptography-library). Protocol/spec/theory documents referenced throughout are from the library's own `docs/` (copies archived in this project's `notes/evidence/`).
[^2]: [Cure53, *Audit-Report: Coinbase cb-mpc Library & Cryptography*, 12.2024 (ref CBS-02)](https://cure53.de/) (report archived in `notes/evidence/cure53-audit.pdf`; bundled in [coinbase/cb-mpc releases](https://github.com/coinbase/cb-mpc/releases)). Finding **CBS-02-004**, "ECC-Refresh-MP vulnerable to small subgroup attacks," is rated **High** severity and concerns the multiparty key-refresh; it, and the other findings (Paillier-keygen checks, a variable-time modular inversion), were reported and — per the report's own fix notes — resolved, with the CBS-02-004 fix explicitly "verified by Cure53."
[^3]: The technical specifics, from Coinbase's own spec and theory documents (`ecdsa-mpc-spec.pdf`, `ecdsa-mpc-theory.pdf`). cb-mpc's multiparty ECDSA is **oblivious-transfer-based** — the Haitner–Lindell–Nof–Ranellucci protocol ([CCS 2018](https://eprint.iacr.org/2018/987)), with the secret-share multiplication carried out over oblivious transfer (Haitner et al., "Highly Efficient OT-Based Multiplication," Eurocrypt 2022); its separate 2-party signer follows [DKLs18](https://www.coinbase.com/blog/fast-secure-2-of-2-ecdsa-using-dkls18). Producing one signature runs to roughly eight machine-to-machine messages once the OT exchanges and their zero-knowledge consistency proofs are counted — the spec's last is literally "Message 8" — where a Schnorr threshold like FROST needs two.
[^4]: Two claims with different strengths. The *signature type* — single-key ECDSA (`P2SH-P2WPKH`/`P2WPKH`), not on-chain multisig or Taproot — is independently verifiable on any Bitcoin node, holds regardless of whose coins these are, and also follows from cb-mpc's design (see [^1], [^3]). That the coins are *IBIT's* rests on Arkham Intelligence's heuristic cluster attribution, unconfirmed by BlackRock or Coinbase, who publish no addresses; the relevant Coinbase entities are Coinbase, Inc. (the trust's "Prime Execution Agent") and Coinbase Custody Trust Company, LLC (the qualified custodian). In one transaction Arkham attributes to that custody (Bitcoin tx `5edda7764a651fe68f2a6f86397b48233e19e09eabc03a941a0aac2799f76e55`, block 957292), ~1,056 BTC moved across six single-key `P2SH-P2WPKH` inputs, each a standard ECDSA (DER) signature with one compressed public key — no Taproot, no Schnorr, no on-chain multisig; a wider pull of 30+ spends from the same cluster showed the same. That establishes the *pattern* (signature type), not the attribution — every sampled spend traces back to that one attributed cluster. And using ECDSA (the scheme shared with Ethereum) rather than Bitcoin-specific Schnorr/Taproot is *suggestive* of the shared multi-asset pipeline, not dispositive. Verified independently on mempool.space and blockstream.info.
[^5]: Komlo & Goldberg, ["FROST: Flexible Round-Optimized Schnorr Threshold Signatures"](https://crysp.uwaterloo.ca/software/frost/frost-extabs.pdf) (2020); [RFC 9591](https://datatracker.ietf.org/doc/rfc9591/) (IETF/CFRG, 2024 — informational, not a formal Internet Standard); [BIP-frost-signing](https://github.com/siv2r/bip-frost-signing), assigned BIP 445 in Jan. 2026, still under active revision; [Bank of Italy's secp256k1-frost](https://github.com/bancaditalia/secp256k1-frost) states it is "not ready for use in a production environment." FROST is genuinely *k*-of-*n* at the protocol level; MuSig2 is n-of-n only.
[^6]: [Unchained, "What is bitcoin multi-institution custody?"](https://www.unchained.com/blog/what-is-bitcoin-multi-institution-custody) and [Onramp, "Multi-Institution Custody"](https://onrampbitcoin.com/products/multi-institution-custody) — Bitcoin-only, native on-chain multisig, no MPC in the stack, client holds the majority of keys.
[^aum]: Coinbase does not publish a per-asset breakdown of what it custodies, but the public figures leave little doubt Bitcoin is by far its largest single holding. U.S. spot Bitcoin ETFs held roughly $92 billion as of April 2026, with funds naming Coinbase as custodian accounting for about $77 billion (~84%) of it; U.S. spot Ethereum ETFs are a small fraction of that, and the hundreds of other supported assets are individually minor. Coinbase reported about $376 billion in "assets on platform" at year-end 2025 and $245.7 billion in institutional assets under custody as of June 30, 2025. A rough estimate of the crypto mix puts Bitcoin near half, Ethereum near a quarter, and everything else sharing the rest — exact internal figures are not public. Sources: [Forbes, "Coinbase Wins OCC Nod For $376 Billion Institutional Custody Empire" (Apr. 8, 2026)](https://www.forbes.com/sites/jasonbrett/2026/04/08/coinbase-wins-occ-nod-for-376-billion-institutional-custody-empire/); [CryptoSlate, "Over 80% of Bitcoin ETF assets hit Coinbase custody choke point"](https://cryptoslate.com/over-80-of-bitcoin-etf-assets-hit-coinbase-custody-choke-point-with-74b-at-risk/). *(Estimate — refine before print.)*
[^7]: [TechCrunch, "Supply chain attack targeting Ledger crypto wallet leaves users hacked," Dec. 14, 2023](https://techcrunch.com/2023/12/14/supply-chain-attack-targeting-ledger-crypto-wallet-leaves-users-hacked/).
[^8]: BlackRock's own SEC filings document the arrangement directly, in near-identical language for both funds. The [iShares Bitcoin Trust (IBIT) FY2025 Form 10-K](https://www.sec.gov/Archives/edgar/data/1980994/000143774926006058/bit20251231_10k.htm) and the [iShares Ethereum Trust (ETHA) FY2025 Form 10-K](https://www.sec.gov/Archives/edgar/data/2000638/000143774926006054/etha20251231_10k.htm) each name "Coinbase Custody Trust Company, LLC" as the custodian for the trust's bitcoin/ether holdings and "The Bank of New York Mellon" as "the custodian for the Trust's cash holdings … and as the administrator of the Trust." Coinbase was BlackRock's sole crypto custodian for both products until Anchorage Digital Bank N.A. was added in April 2025 as an "additional available custodian" (so listed in both 10-Ks; see also [Bloomberg](https://www.bloomberg.com/news/articles/2025-04-08/blackrock-adds-anchorage-digital-as-crypto-custodian)). See Part 4 of this series for the IBIT custody/verification arguments in full.
[^balchunas]: Eric Balchunas (Bloomberg), [post on X, Sept. 24, 2024](https://x.com/EricBalchunas/status/1838296036772593716): BlackRock runs its own Bitcoin node and checks IBIT balances against Coinbase Prime addresses nightly, shown to institutional clients on request but not published — "because they get plenty of spam already (eg sanctioned bitcoin, NFTs) and this would only cause an explosion in said spam." (Verbatim text recovered via X's indexed page metadata; X blocks automated fetch. Same source as Part 4's [^7].)
[^bitgo]: [BitGo, "Multi-Sig Wallets for Institutions"](https://www.bitgo.com/resources/blog/multi-sig-wallets-for-institutions/) — native on-chain 2-of-3 multisig for Bitcoin, marketed as "transparent on-chain validation and audit trails," explicitly in contrast to MPC. (BitGo also runs a parallel MPC/TSS product for chains without native multisig — the same asset-diversity pressure described here.)
[^redoctober]: The two-key nuclear-launch trope is the cultural shorthand for this guarantee: in *The Hunt for Red October* (1990), the missile keys are positioned so that one person cannot turn both at once. Note what it does and doesn't do — it makes *solo* action impossible while saying nothing about whether the order *should* be carried out. That second question, the human one, is Part 3's.
[^safu]: "Safu" is crypto-community slang for "safe," originating from a 2018 incident: Binance CEO Changpeng Zhao ("CZ") tweeted reassurances like "Funds are safe" (and, in a later post, "funds are safu") during a system upgrade. A YouTuber named Bizonacci turned this into a satirical viral video, "Funds Are Safu," and the misspelling stuck as shorthand across the industry — often used earnestly, sometimes ironically, given its origin as a reassurance made during a moment of user uncertainty. Binance later formalized "SAFU" as an actual name, the "Secure Asset Fund for Users," a real insurance-style reserve launched in July 2018.
